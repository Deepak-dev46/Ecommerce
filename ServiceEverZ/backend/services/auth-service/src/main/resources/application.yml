server:
  port: 0
spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://mysql:3306/serviceeverz?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-service:8761/eureka/

  instance:
    prefer-ip-address: true

jwt.secret: thisISJustaSupewe2323erKey@11938238
jwt.expiration: 1821982